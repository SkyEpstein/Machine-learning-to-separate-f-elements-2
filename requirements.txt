numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
rdkit>=2023.9
openpyxl>=3.1
# TabPFN: use the local v2 line, not the v3 cloud client (v3 sends data to a remote API)
tabpfn<3
# optional, only for the graph-network experiment
# chemprop>=1.6
